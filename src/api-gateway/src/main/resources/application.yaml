server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always
    whitelabel:
      enabled: false

# JWT Configuration - must be at root level
app:
  jwt:
    jwks-uri: ${JWT_JWKS_URI:http://kms/quan-ly-khoa/.well-known/jwks.json}
    jwks-cache-seconds: ${JWT_JWKS_CACHE_SECONDS:300}
  security:
    token-blacklist:
      l1:
        max-size: ${TOKEN_BLACKLIST_L1_MAX_SIZE:10000}
        ttl-seconds: ${TOKEN_BLACKLIST_L1_TTL_SECONDS:30}
  rate-limit:
    login:
      limit: ${APP_RATE_LIMIT_LOGIN_LIMIT:3}
      window-seconds: ${APP_RATE_LIMIT_LOGIN_WINDOW_SECONDS:5}
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    prometheus:
      access: unrestricted
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: ${MANAGEMENT_ZIPKIN_TRACING_ENDPOINT:http://jaeger:9411/api/v2/spans}
spring:
  application:
    name: api-gateway
  threads:
    virtual:
      enabled: true
  cloud:
    gateway:
      server:
        webflux:
          # Eureka Discovery Routes
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true

          # Default filters
          default-filters:
            - name: Retry
              args:
                retries: 3
                statuses: SERVICE_UNAVAILABLE,BAD_GATEWAY,GATEWAY_TIMEOUT
                methods: GET,HEAD
                backoff:
                  firstBackoff: 100ms
                  maxBackoff: 500ms
                  factor: 2
                  basedOnPreviousValue: false
            - name: AddRequestHeader
              args:
                name: X-Internal-Secret
                value: "${INTERNAL_SECRET}"
            - name: DedupeResponseHeader
              args:
                name: Access-Control-Allow-Origin
                strategy: RETAIN_FIRST
            - name: DedupeResponseHeader
              args:
                name: Access-Control-Allow-Credentials
                strategy: RETAIN_FIRST

          # Routes configuration
          routes:
            # Actuator health - public, no auth needed
            - id: actuator-health-route
              uri: http://localhost:8080
              predicates:
                - Path=/actuator/health
              metadata:
                requires-jwt: false

            # Eureka Dashboard Route - Forward to HAProxy load balancer
            - id: eureka-dashboard-route
              uri: http://localhost:8760
              predicates:
                - Path=/eureka/**
              metadata:
                requires-jwt: false

            # KMS Service Route
            - id: kms-service-route
              uri: lb://kms
              predicates:
                - Path=/quan-ly-khoa/**
              filters:
                - StripPrefix=1
              metadata:
                requires-jwt: false

            # Public IAM endpoints - no JWT required
            - id: auth-public-route
              uri: ${AUTH_SERVICE_URL:lb://auth-service}
              predicates:
                - Path=/api/xac-thuc/dang-nhap,/api/xac-thuc/dang-ky,/api/xac-thuc/dang-xuat,/api/xac-thuc/oauth2/token,/api/xac-thuc/2fa/**
              filters:
                - StripPrefix=1
              metadata:
                requires-jwt: false

            # Protected IAM endpoints - JWT required
            - id: auth-protected-route
              uri: ${AUTH_SERVICE_URL:lb://auth-service}
              predicates:
                - Path=/api/xac-thuc/kiem-tra,/api/xac-thuc/doi-mat-khau,/api/xac-thuc/tai-khoan/**,/api/xac-thuc/quan-tri/**,/api/xac-thuc/nhat-ky/**,/api/xac-thuc/trang-thai-dong-bo/**,/api/xac-thuc/thu-lai-dong-bo/**
              filters:
                - StripPrefix=1
              metadata:
                requires-jwt: true

            # HR Service Routes - Employee
            # StripPrefix=1 strips /api → /nhan-vien/** hits EmployeeController @RequestMapping("/nhan-vien")
            - id: hr-employee-route
              uri: ${HR_SERVICE_URL:lb://hr-service}
              predicates:
                - Path=/api/nhan-vien/**
              filters:
                - StripPrefix=1
              metadata:
                requires-jwt: true
                requires-hmac: false

            # HR Service Routes - Department
            # StripPrefix=1 strips /api → /phong-ban/** hits DepartmentController @RequestMapping("/phong-ban")
            - id: hr-department-route
              uri: ${HR_SERVICE_URL:lb://hr-service}
              predicates:
                - Path=/api/phong-ban/**
              filters:
                - StripPrefix=1
              metadata:
                requires-jwt: true
                requires-hmac: false

            # HR Service Routes - Organization Unit
            # RewritePath maps /api/don-vi-to-chuc/** → /organization-units/** (controller path)
            - id: hr-org-unit-route
              uri: ${HR_SERVICE_URL:lb://hr-service}
              predicates:
                - Path=/api/don-vi-to-chuc/**
              filters:
                - RewritePath=/api/don-vi-to-chuc/(?<segment>.*), /organization-units/$\{segment}
                - RewritePath=/api/don-vi-to-chuc, /organization-units
              metadata:
                requires-jwt: true
                requires-hmac: false

            # HR Service Routes - Documents
            - id: hr-documents-route
              uri: ${HR_SERVICE_URL:lb://hr-service}
              predicates:
                - Path=/api/hr/documents/**
              filters:
                - StripPrefix=2
              metadata:
                requires-jwt: true
                requires-hmac: false

            # HR Service Routes - Browser API (English)
            - id: hr-browser-api-route
              uri: ${HR_SERVICE_URL:lb://hr-service}
              predicates:
                - Path=/api/hr/**
              filters:
                - StripPrefix=2
              metadata:
                requires-jwt: true
                requires-hmac: false

            # HR Service Routes - Payroll
            - id: hr-payroll-route
              uri: ${HR_SERVICE_URL:lb://hr-service}
              predicates:
                - Path=/api/payroll/**,/api/chi-tra/**
              metadata:
                requires-jwt: true
                requires-hmac: false

            # Project Service Routes
            - id: project-browser-api-route
              uri: ${PROJECT_SERVICE_URL:lb://project-service}
              predicates:
                - Path=/api/projects/**
              metadata:
                requires-jwt: true
                requires-hmac: false

            # Project Service Routes - Vietnamese legacy path
            - id: project-service-route
              uri: ${PROJECT_SERVICE_URL:lb://project-service}
              predicates:
                - Path=/api/du-an/**
              filters:
                - RewritePath=/api/du-an/(?<segment>.*), /api/projects/$\{segment}
              metadata:
                requires-jwt: true
                requires-hmac: false

            # Task Service Routes
            - id: task-browser-api-route
              uri: ${TASK_SERVICE_URL:lb://task-service}
              predicates:
                - Path=/api/tasks/**
              metadata:
                requires-jwt: true
                requires-hmac: false

            # Task Service Routes - Vietnamese legacy path
            - id: task-service-route
              uri: ${TASK_SERVICE_URL:lb://task-service}
              predicates:
                - Path=/api/cong-viec/**
              filters:
                - RewritePath=/api/cong-viec/(?<segment>.*), /api/tasks/$\{segment}
              metadata:
                requires-jwt: true
                requires-hmac: false

            # AI Suggestions & Risk Radar - forward to task-service
            - id: ai-suggestions-route
              uri: ${TASK_SERVICE_URL:lb://task-service}
              predicates:
                - Path=/api/v1/ai/**
              filters:
                - StripPrefix=1
              metadata:
                requires-jwt: true
                requires-hmac: false

            # Automation Rules - forward to task-service
            - id: automation-rules-route
              uri: ${TASK_SERVICE_URL:lb://task-service}
              predicates:
                - Path=/api/v1/automation/**
              filters:
                - StripPrefix=1
              metadata:
                requires-jwt: true
                requires-hmac: false

  data:
    redis:
      host: ${REDIS_HOST:127.0.0.1}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${JWT_JWKS_URI:http://kms/quan-ly-khoa/.well-known/jwks.json}

---
# Docker Profile Configuration
spring:
  config:
    activate:
      on-profile: docker
  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}

# Eureka Client Configuration
eureka:
  client:
    enabled: true
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE:http://${EUREKA_USERNAME}:${EUREKA_PASSWORD}@eureka-peer1:${EUREKA_PEER1_PORT}/eureka/,http://${EUREKA_USERNAME}:${EUREKA_PASSWORD}@eureka-peer2:${EUREKA_PEER2_PORT}/eureka/,http://${EUREKA_USERNAME}:${EUREKA_PASSWORD}@eureka-peer3:${EUREKA_PEER3_PORT}/eureka/}
    registry-fetch-interval-seconds: 5
  instance:
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 10
    lease-expiration-duration-in-seconds: 30
    metadata-map:
      zone: us-east-1a
      version: 1.0.0
      description: API Gateway for microservices

app:
  jwt:
    jwks-uri: ${JWT_JWKS_URI:http://kms-service:8083/quan-ly-khoa/.well-known/jwks.json}
    jwks-cache-seconds: ${JWT_JWKS_CACHE_SECONDS:300}

logging:
  level:
    "[org.springframework.cloud.gateway]": INFO
    "[reactor.netty.http.server]": INFO
    "[com.netflix.eureka]": INFO
    "[com.netflix.discovery]": INFO

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
